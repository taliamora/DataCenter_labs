CREATE TABLE outcomes (
    "Animal ID" VARCHAR,
    "DateTime" TIMESTAMP,
    "Date of Birth" DATE,
    "Outcome Type" VARCHAR,
    "Animal Type" VARCHAR,
    "Breed" VARCHAR,
    "Color" VARCHAR,
    month VARCHAR,
    year INT,
    "reproductive_status" VARCHAR,
    "sex" VARCHAR);